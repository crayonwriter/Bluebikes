This query returns a table that shows the stations and their names.

SELECT DISTINCT ST.NAME,
	ST.DISTRICT,
	ST.ID AS START_STATION_ID,
	ST.ID
FROM BLUEBIKES_STATIONS ST
JOIN BLUEBIKES_2019 B2019 ON ST.ID = B2019.START_STATION_ID
ORDER BY 4;